/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['api.cateringsandtiffins.in'], // Add your external domain here
  },
};

export default nextConfig;
